import { Card, styled } from '@mui/material';

export * from '@/common/style';

export const QrContainer = styled(Card)(({}) => ({
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'center',
}));
