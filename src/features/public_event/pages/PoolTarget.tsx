import { FC } from 'react';
import { EventModel } from '@/types/models/event/event.model';

interface PoolTargetProps {
  event: EventModel;
}

export const PoolTarget: FC<PoolTargetProps> = ({}) => {
  return <div>PoolTarget</div>;
};
